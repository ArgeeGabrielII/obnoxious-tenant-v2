.card.card-icon.mb-4
    .row.gx-0
        .col-auto.card-icon-aside(
            [ngClass]='iconBackground'
        )
            ng-content(select='.card-icon')
        .col
            ng-content(select='.card-body')